services:
  app:
    build: .
    container_name: milkymarble_app
    restart: always
    ports:
      - "8001:3000"
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules